def foo(b: str = "bar"):
    """Stupid example of shared package

    Args:
        b (str, optional):
            _description_.
            Defaults to "bar".

    Returns:
        _type_:
            _description_
    """
    return f"foo {b}"
